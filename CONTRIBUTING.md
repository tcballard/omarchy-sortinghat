# Contributing

Open an exact issue before implementation. Keep QML thin and add deterministic temporary-tree tests for every filesystem safety change. Never use real personal files as fixtures.

Before proposing a change, run `./scripts/verify.sh`. Record blocked checks honestly. Commits should be focused; generated packages and `target/` do not belong in Git.

